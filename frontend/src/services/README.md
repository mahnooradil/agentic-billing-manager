# services/

Client-side API layer. Wraps calls to the backend REST API (via a typed
`fetch`/axios client) so components never talk to the network directly.

No services are implemented in Phase 1 — this is structure only.
