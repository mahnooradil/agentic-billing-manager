# features/

Feature-based modules. Each feature owns its own components, hooks, and logic
(e.g. `auth/`, `dashboard/`, `billing/`).

Group by domain feature, not by file type, to keep the codebase scalable.
Nothing is implemented in Phase 1 — this is structure only.
