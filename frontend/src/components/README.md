# components/

Reusable, presentational UI components shared across the app.

- Keep components **dumb/presentational** where possible (props in, UI out).
- `ui/` will hold shadcn/ui primitives once added.
- Feature-specific components live under `features/` instead.
